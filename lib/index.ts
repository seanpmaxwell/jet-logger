import { JetLogger } from './JetLogger';


export {
  LoggerModes,
  Formats,
  TCustomLogger,
  JetLogger,
} from './JetLogger';

// Default is logger with no manual settings
export default JetLogger();
